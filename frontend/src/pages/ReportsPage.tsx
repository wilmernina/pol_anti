import { useState } from 'react';
import { PortalLayout } from '../components/PortalLayout';

export function ReportsPage({ type }: { type: 'general' | 'ejes' }) {
  const [gestion, setGestion] = useState(2026); const [eje, setEje] = useState('1');
  return <PortalLayout><main><div className="mb-6"><p className="text-sm font-semibold uppercase tracking-wide text-emerald-700">Reportes</p><h1 className="mt-1 text-3xl font-bold">{type === 'general' ? 'Reporte General' : 'Reporte por Ejes'}</h1><p className="mt-1 text-slate-500">Generación de información para seguimiento institucional</p></div><section className="max-w-2xl rounded-xl bg-white p-6 shadow-sm ring-1 ring-slate-200">{type === 'general' ? <><div className="mb-5 grid h-14 w-14 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><span className="text-2xl">▤</span></div><h2 className="text-xl font-bold">Resumen general anual</h2><p className="mt-2 text-sm text-slate-500">Genera un documento PDF con el resumen de la Política Antidroga por gestión.</p><label className="mt-6 block text-sm font-semibold">Gestión<select value={gestion} onChange={(e) => setGestion(Number(e.target.value))} className="mt-2 block rounded-lg border px-3 py-2">{[2026, 2027, 2028, 2029, 2030].map((year) => <option key={year}>{year}</option>)}</select></label><a className="mt-6 inline-flex rounded-lg bg-emerald-700 px-4 py-2 font-semibold text-white" href={`/api/reportes/anual/${gestion}`}>Generar PDF general</a></> : <><div className="mb-5 grid h-14 w-14 place-items-center rounded-xl bg-blue-50 text-blue-700"><span className="text-2xl">◈</span></div><h2 className="text-xl font-bold">Matriz detallada por eje</h2><p className="mt-2 text-sm text-slate-500">Exporta las acciones, metas y avances de un eje en formato Excel.</p><label className="mt-6 block text-sm font-semibold">Eje<select value={eje} onChange={(e) => setEje(e.target.value)} className="mt-2 block rounded-lg border px-3 py-2">{Array.from({ length: 9 }, (_, index) => index + 1).map((codigo) => <option key={codigo} value={codigo}>Eje {codigo}</option>)}</select></label><a className="mt-6 inline-flex rounded-lg bg-blue-700 px-4 py-2 font-semibold text-white" href={`/api/reportes/eje/${eje}/excel`}>Exportar Excel del eje</a></>}</section></main></PortalLayout>;
}
